<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="MeterRecti.Native.Pages.SettingsPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:viewModels="clr-namespace:MeterRecti.Native.ViewModels"
    x:DataType="viewModels:SettingsViewModel"
    Title="MQTT"
    BackgroundColor="{StaticResource Surface}">

    <ScrollView>
        <VerticalStackLayout Padding="20,18" Spacing="18">
            <VerticalStackLayout Spacing="4">
                <Label Text="Connection" TextColor="{StaticResource TextSecondary}" FontSize="13" />
                <Label Text="MQTT 设置" TextColor="{StaticResource TextPrimary}" FontSize="28" FontAttributes="Bold" />
            </VerticalStackLayout>

            <Border Stroke="{StaticResource Divider}" StrokeThickness="1" BackgroundColor="White" Padding="16">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="14" />
                </Border.StrokeShape>
                <VerticalStackLayout Spacing="12">
                    <Label Text="{Binding StatusText}" TextColor="{Binding StatusColor}" FontAttributes="Bold" />
                    <Entry Text="{Binding Host}" Placeholder="Broker 地址，例如 broker.example.com" Keyboard="Url" />
                    <Entry Text="{Binding Port}" Placeholder="端口，例如 8883" Keyboard="Numeric" />
                    <HorizontalStackLayout Spacing="10">
                        <Switch IsToggled="{Binding UseTls}" />
                        <Label Text="启用 TLS" VerticalTextAlignment="Center" TextColor="{StaticResource TextPrimary}" />
                    </HorizontalStackLayout>
                    <Entry Text="{Binding Username}" Placeholder="用户名" />
                    <Entry Text="{Binding Password}" Placeholder="密码" IsPassword="True" />
                    <Entry Text="{Binding SubscribeTopic}" Placeholder="订阅 Topic 基础路径" />
                    <Entry Text="{Binding PublishTopic}" Placeholder="发布 Topic 基础路径" />
                    <Grid ColumnDefinitions="*,*" ColumnSpacing="12">
                        <Button Text="保存并连接" Command="{Binding SaveAndConnectCommand}" BackgroundColor="{StaticResource Periwinkle}" TextColor="{StaticResource TextPrimary}" FontAttributes="Bold" />
                        <Button Grid.Column="1" Text="断开" Command="{Binding DisconnectCommand}" BackgroundColor="{StaticResource Mist}" TextColor="{StaticResource TextPrimary}" />
                    </Grid>
                </VerticalStackLayout>
            </Border>
            <Label Text="{Binding AppIdentity}" TextColor="{StaticResource TextSecondary}" FontSize="12" />
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
